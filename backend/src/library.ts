export function encodeHTML(input: string[]): string[] {
  const cleanValues: string[] = [];
  // The starter project only defines the empty-input behaviour for now.
  return input.length === 0 ? cleanValues : cleanValues;
}
